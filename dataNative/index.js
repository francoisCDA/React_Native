/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import AppGPS from './AppGPS';

AppRegistry.registerComponent(appName, () => AppGPS);
